<html>
<head>
    <style type="text/css" media="screen">
        body {
            padding: 40px;
        }
        p {
            padding: 10px;
        }

        code {
            font-weight: bold;
        }
    </style>
</head>
<body>
    <h1>Docker Test</h1>
    <p>Press the following link to confirm that your Docker install is fully setup.</p>
    <p>If so, then run <code>./site_stop.sh</code> (<code>./site_stop.bat</code> on Windows).</p>
    <p>Then modify lines 7 &amp; 14 in docker-compose.override.yml to point to your actual project folder and rerun <code>./site_run.sh</code></p>
    <p><a href="sub/index.php">PHP Info</a></p>
</body>
</html>
